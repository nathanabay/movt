#!/usr/bin/expect -f
set timeout 120
spawn ssh -i /Users/nathanamare/Downloads/bespoet/besponate bespoet@bespo.et "cd /home/bespoet/repositories/movt && git reset --hard && git clean -fd && git pull && mkdir -p tmp && touch tmp/restart.txt"
expect "*assword:*"
send "aND1AND12$\r"
expect eof
